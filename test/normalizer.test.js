import test from 'node:test';import assert from 'node:assert/strict';import demo from '../demo-data.js';import{normalizeGame}from'../normalizer.js';test('normalizes demo game',()=>{const s=normalizeGame(demo,{teams:{away:{},home:{}}});assert.equal(s.score.away,3);assert.equal(s.score.home,4);assert.equal(s.situation.inning,5);assert.equal(s.situation.bases.first,true);assert.equal(s.lineups.home.length,3)});